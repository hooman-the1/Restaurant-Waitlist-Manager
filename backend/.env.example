PORT=8000
SECRET_KEY=replace-with-a-local-signing-secret
FRONTEND_ORIGIN=http://localhost:4200
# Use PostgreSQL instead when a local server is available:
# DATABASE_URL=postgresql://waitlist:waitlist@localhost:5432/waitlist
DATABASE_URL=sqlite://./data/waitlist.sqlite
